# Phase 0 (court + feet) — lightweight, runs on Apple Silicon CPU/MPS
yt-dlp
opencv-python
numpy
duckdb
polars
pyyaml

# Detection/pose (heavy; installs torch + MPS)
ultralytics              # YOLO11-Pose (AGPL-3.0 — prototype only; migrate to RTMPose later)

# Phase 1 — analytics dashboard
streamlit
altair
pandas
matplotlib

# Commentary layer (LLM match reports)
anthropic
