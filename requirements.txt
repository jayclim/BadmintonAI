# Phase 0 (court + feet) — lightweight, runs on Apple Silicon CPU/MPS
yt-dlp
opencv-python
numpy
duckdb
polars
pyyaml

# Detection/pose (heavy; installs torch + MPS)
ultralytics              # YOLO11-Pose (AGPL-3.0 — prototype only; migrate to RTMPose later)

# Phase 1 — analytics dashboard
streamlit
altair
pandas
matplotlib

# Commentary layer (LLM match reports; keys in .env — GEMINI_API_KEY / ANTHROPIC_API_KEY)
anthropic
requests

# Phase 2 — shuttle tracking (TrackNetV3 vendored in third_party/) + shot classes
gdown                    # fetch TrackNetV3/BST checkpoints from Google Drive
parse                    # TrackNetV3 dep
pycocotools              # TrackNetV3 dep (via its test.py)
scikit-learn             # geometry shot-class baseline (shotclass.py)
positional-encodings     # BST dep (bst_eval.py via third_party/BST)
torchinfo                # BST dep
